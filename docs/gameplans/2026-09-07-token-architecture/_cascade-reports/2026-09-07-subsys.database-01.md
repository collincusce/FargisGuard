# Cascade Report: subsys.database version 0.3.0 → 0.4.0: append_pending_reason added; no schema change

> Generated: 2026-09-07T00:51:30Z

## Trigger

`subsys.database` — version 0.3.0 → 0.4.0: append_pending_reason added; no schema change

## Affected entities

### Direct dependents

- **subsys.appeals** (status: active) — checked: no change needed — additive function only, no schema change
- **subsys.bot-gateway** (status: active) — checked: no change needed
- **subsys.dashboard** (status: active) — checked: no change needed — pending_actions rows unchanged in shape
- **subsys.moderation** (status: active) — checked: updated this phase — uses append_pending_reason; pin database ^0.4
- **subsys.rules** (status: active) — checked: no change needed

### Transitive dependents

- **subsys.ai-engine** — flagged via upstream; checked: no change needed
- **subsys.batcher** — flagged via upstream; checked: no change needed — does not touch the database
- **subsys.pipeline** — flagged via upstream; checked: no change needed — goes through moderation

## Updates applied

database.py: append_pending_reason; docs/subsystems/database.md note; version 0.4.0

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
