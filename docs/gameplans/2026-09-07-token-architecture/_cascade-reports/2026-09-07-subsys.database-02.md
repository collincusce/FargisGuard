# Cascade Report: subsys.database version 0.4.0 → 0.5.0: batch_settings table added to SCHEMA (additive)

> Generated: 2026-09-07T00:54:06Z

## Trigger

`subsys.database` — version 0.4.0 → 0.5.0: batch_settings table added to SCHEMA (additive)

## Affected entities

### Direct dependents

- **subsys.appeals** (status: active) — checked: no change needed — additive table
- **subsys.batcher** (status: active) — checked: updated this phase — batchsettings reads/writes batch_settings; pin database ^0.5
- **subsys.bot-gateway** (status: active) — checked: updated this phase — /batch commands; pin database ^0.5
- **subsys.dashboard** (status: active) — checked: no change needed
- **subsys.moderation** (status: active) — checked: no change needed — additive table only
- **subsys.rules** (status: active) — checked: no change needed

### Transitive dependents

- **subsys.ai-engine** — flagged via upstream; checked: no change needed
- **subsys.pipeline** — flagged via upstream; checked: no change needed — reads the interval through the injected interval_for

## Updates applied

database.py SCHEMA: batch_settings table; version 0.5.0

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
