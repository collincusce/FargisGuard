# Cascade Report: subsys.database 0.1.0 -> 0.2.0 (module-level conn/cursor removed; connect() context manager; pending_actions table; appeals columns)

> Generated: 2026-09-06T12:10:55Z

## Trigger

`subsys.database` — 0.1.0 -> 0.2.0 (module-level conn/cursor removed; connect() context manager; pending_actions table; appeals columns)

## Affected entities

### Direct dependents

- **subsys.appeals** (status: active) — checked: updated in Phase 4 — call sites use connect()/new punish contract; depends_on pins bumped; tests green
- **subsys.bot-gateway** (status: active) — checked: updated in Phase 4 — call sites use connect()/new punish contract; depends_on pins bumped; tests green
- **subsys.dashboard** (status: active) — checked: DEFERRED to Phase 5 — dashboard.py still imports the removed database.cursor and cannot import until migrated (H-08 partial)
- **subsys.moderation** (status: active) — checked: updated in Phase 4 — call sites use connect()/new punish contract; depends_on pins bumped; tests green
- **subsys.rules** (status: active) — checked: updated in Phase 4 — call sites use connect()/new punish contract; depends_on pins bumped; tests green

### Transitive dependents

- **subsys.ai-engine** — flagged via upstream; checked: no change needed — reaches database only through rules/moderation, whose public functions kept their signatures
- **subsys.pipeline** — flagged via upstream; checked: no change needed — reaches database only through rules/moderation, whose public functions kept their signatures

## Updates applied

rules, appeals, moderation, bot-gateway migrated to connect(); transitive dependents unaffected

## Updates deferred

subsys.dashboard: migrate dashboard.py to connect() in Phase 5 (task 5.2)
