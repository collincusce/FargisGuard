# Cascade Report: subsys.rules version 0.3.0 → 0.4.0: composer.py (compose_rules, RulesResolver, get_resolver) and rules.snapshot added; now depends on channels for ScopeChain

> Generated: 2026-09-06T13:05:29Z

## Trigger

`subsys.rules` — version 0.3.0 → 0.4.0: composer.py (compose_rules, RulesResolver, get_resolver) and rules.snapshot added; now depends on channels for ScopeChain

## Affected entities

### Direct dependents

- **subsys.ai-engine** (status: active) — checked: no change needed yet — still reads get_rules(guild_id); Phase 6 switches analyze_message to composer.get_resolver()
- **subsys.bot-gateway** (status: completed) — checked: no change needed yet — Phase 5 adds the /rules group that calls set/clear_scope_rules and compose_rules

### Transitive dependents

- **subsys.pipeline** — flagged via upstream; checked: no change needed — does not import rules or composer

## Updates applied

composer.py added; rules.snapshot added; docs/subsystems/rules.md composition section; version 0.4.0 with new dependency on subsys.channels

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
