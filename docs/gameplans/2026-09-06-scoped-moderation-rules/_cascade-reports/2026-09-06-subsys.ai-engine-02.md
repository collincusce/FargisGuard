# Cascade Report: subsys.ai-engine version 0.3.0 → 0.4.0: analyze_message resolves scoped rules via composer.get_resolver(); classify logs ruleset key, sizes, and API token usage at DEBUG; depends on rules ^0.4

> Generated: 2026-09-06T13:11:23Z

## Trigger

`subsys.ai-engine` — version 0.3.0 → 0.4.0: analyze_message resolves scoped rules via composer.get_resolver(); classify logs ruleset key, sizes, and API token usage at DEBUG; depends on rules ^0.4

## Affected entities

### Direct dependents

- **subsys.bot-gateway** (status: active) — checked: no change needed — wires analyze_message by reference; new resolver kwarg is optional; pin ^0.3 still satisfied (additive)
- **subsys.pipeline** (status: active) — checked: no change needed — already passes scope=; pin ^0.3 still satisfied (additive)

### Transitive dependents

- _(none)_

## Updates applied

ai_engine.py: resolver path in analyze_message, DEBUG measurement line in classify; config.LOG_LEVEL; bot.main logging setup; fakes usage; 5 tests; ARCHITECTURE/DEPLOYMENT/CHANGELOG/ai-engine doc; version 0.4.0

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
