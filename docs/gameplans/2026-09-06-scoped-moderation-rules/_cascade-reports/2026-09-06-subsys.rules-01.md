# Cascade Report: subsys.rules version 0.2.0 → 0.3.0: scoped API added; get_rules/set_rules signatures unchanged, now guild-scope with legacy mirror

> Generated: 2026-09-06T12:56:06Z

## Trigger

`subsys.rules` — version 0.2.0 → 0.3.0: scoped API added; get_rules/set_rules signatures unchanged, now guild-scope with legacy mirror

## Affected entities

### Direct dependents

- **subsys.ai-engine** (status: active) — checked: no change needed — get_rules(guild_id) signature unchanged; test_ai_engine loader test passes unchanged; scope-aware analyze is Phase 6
- **subsys.bot-gateway** (status: completed) — checked: no change needed — set_rules(guild_id, content) signature unchanged; scope authoring commands are Phase 5

### Transitive dependents

- **subsys.pipeline** — flagged via upstream; checked: no change needed — does not import rules

## Updates applied

rules.py rewritten: scope constants, validate_scope, get/set/clear/list scope rules, get_rules_version; get_rules/set_rules preserved as guild-scope wrappers with legacy-table mirror. tests/test_rules.py added (15 tests). docs/subsystems/rules.md body written, version 0.3.0.

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
