# Cascade Report: subsys.database version 0.2.0 → 0.3.0: scoped_rules, rules_version, schema_migrations tables; SCRIPTS one-shot migration layer

> Generated: 2026-09-06T12:56:04Z

## Trigger

`subsys.database` — version 0.2.0 → 0.3.0: scoped_rules, rules_version, schema_migrations tables; SCRIPTS one-shot migration layer

## Affected entities

### Direct dependents

- **subsys.appeals** (status: active) — checked: no change needed — appeals table and API untouched; schema change is additive (new tables only)
- **subsys.bot-gateway** (status: completed) — checked: no change needed now — /setrules still calls set_rules(guild_id, content); scope commands arrive in Phase 5
- **subsys.dashboard** (status: active) — checked: no change needed — reads warnings/appeals/pending_actions only
- **subsys.moderation** (status: active) — checked: no change needed — warnings/pending_actions untouched
- **subsys.rules** (status: active) — checked: updated this phase to 0.3.0, pin raised to subsys.database@^0.3

### Transitive dependents

- **subsys.ai-engine** — flagged via upstream; checked: no change needed — still calls get_rules(guild_id); scope wiring is Phase 6
- **subsys.pipeline** — flagged via upstream; checked: no change needed — no database access; scope resolution is Phase 2

## Updates applied

database.py: scoped_rules, rules_version, schema_migrations tables; SCRIPTS layer + _apply_script; backfill script. docs/subsystems/database.md body written, version 0.3.0.

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
