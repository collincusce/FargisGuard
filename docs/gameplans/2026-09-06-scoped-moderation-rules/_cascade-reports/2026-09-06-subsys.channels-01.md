# Cascade Report: subsys.channels version 0.1.0 → 0.2.0: ScopeChain, ScopeError, resolve_scope added; is_exempt unchanged

> Generated: 2026-09-06T13:00:03Z

## Trigger

`subsys.channels` — version 0.1.0 → 0.2.0: ScopeChain, ScopeError, resolve_scope added; is_exempt unchanged

## Affected entities

### Direct dependents

- **subsys.pipeline** (status: active) — checked: updated this phase to 0.3.0; pin raised to subsys.channels@^0.2; calls resolve_scope inside the fail-closed boundary

### Transitive dependents

- **subsys.bot-gateway** — flagged via upstream; checked: no change needed — imports only Deps/handle_message; analyze wiring passes through the new kwarg

## Updates applied

channels.py: ScopeChain, ScopeError, resolve_scope; docs/subsystems/channels.md body; version 0.2.0

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
